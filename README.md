# etl-pipeline
ETL pipeline built with Python, pandas and PostgreSQL. Processes transaction data through a layered architecture: raw ingestion → transformation &amp; validation → final load. Fully containerized with Docker.
